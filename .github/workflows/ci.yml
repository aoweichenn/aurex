name: CI

on:
  push:
  pull_request:

jobs:
  coverage:
    runs-on: ubuntu-24.04
    container:
      image: fedora:latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          dnf install -y clang cmake gcc-c++ git gtest-devel llvm llvm-devel make
      - name: Configure tool paths
        run: |
          {
            echo "LLVM_COV=$(command -v llvm-cov)"
            echo "LLVM_PROFDATA=$(command -v llvm-profdata)"
            echo "CC=$(command -v clang)"
            echo "CXX=$(command -v clang++)"
          } >> "${GITHUB_ENV}"
      - name: Check coverage
        env:
          AUREX_COVERAGE_LINE_THRESHOLD: "90"
          AUREX_COVERAGE_FUNCTION_THRESHOLD: "90"
          AUREX_COVERAGE_REGION_THRESHOLD: "90"
        run: tools/check_coverage.sh
