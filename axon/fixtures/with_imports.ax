module with_imports;

import imported.a;
import imported.b;

extern c {
    fn puts(s: *const u8) -> i32 @name("puts");
}

export c fn main(argc: i32, argv: *mut *mut u8) -> i32 {
    puts(c"imports test");
    return 0;
}
