module match_expression_duplicate;

enum Choice: u8 {
    zero = 0,
    one = 1,
}

fn main() -> i32 {
    return match Choice_zero {
        Choice_zero => 0,
        Choice_zero => 1,
        Choice_one => 2,
    };
}
