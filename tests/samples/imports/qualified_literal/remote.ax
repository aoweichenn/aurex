module qualified_literal.remote;

pub struct Box {
  pub value: bool;
}

pub struct PairI32 {
  pub value: i32;
}

pub enum ResultI32I32: u8 {
  ok(i32) = 1,
  err(i32) = 2,
}

pub enum Mode: u8 {
  fast = 1,
  slow = 2,
}

pub enum MaybeI32: u8 {
  none = 0,
  some(i32) = 1,
}
