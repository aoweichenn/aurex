module generic_alignof_missing_sized;

fn bad<T>(value: T) -> usize {
    if ptraddr(&value) == 0usize {
        return 0usize;
    }
    return alignof<T>();
}

fn main() -> i32 {
    return ((bad<i32>(1)) as i32);
}
