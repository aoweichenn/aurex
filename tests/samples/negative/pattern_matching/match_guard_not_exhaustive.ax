module match_guard_not_exhaustive;

enum Choice: u8 {
    one = 1,
}

fn score(enabled: bool) -> i32 {
    return match Choice.one {
        .one if enabled => 1,
    };
}

fn main() -> i32 {
    return score(false);
}
