module deref_opaque_pointer;

extern c {
    opaque struct Handle;
}

fn bad() -> void {
    let handle: *mut Handle = paddr(*mut Handle, 0);
    return *handle;
}

fn main() -> i32 {
    return 0;
}
