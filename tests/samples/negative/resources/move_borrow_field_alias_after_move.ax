module move_borrow_field_alias_after_move;

struct Box<T> {
    value: T;
}

fn consume<T>(value: Box<T>) -> void {
}

fn inspect<T>(value: &T) -> void {
}

fn duplicate<T>(box: Box<T>) -> void {
    let borrowed: &T = &box.value;
    consume(box);
    inspect(borrowed);
}

fn main() -> i32 {
    return 0;
}
