module reexport_import;

import samplelib.reexport_facade;

fn main() -> i32 {
    let value: Count = add_two(0);
    let pair: Pair<i32> = Pair<i32> {
        left: value,
        right: base,
    };
    let mode = Mode.ready;
    if mode != Mode_ready {
        return 1;
    }
    return pair.left + pair.right - 82;
}

